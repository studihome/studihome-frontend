name: Refine Studio AI card and Dapur UX
on:
  push:
    branches: [feat/studio-ai-creator-card-dapur-profile-v2]
  workflow_dispatch:
permissions:
  contents: write
jobs:
  refine:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply scoped UI and UX refinements
        shell: python
        run: |
          from pathlib import Path
          root=Path('.')
          studio=root/'studio-ai-creator-card.js'
          studio.write_text("""(()=>{'use strict';if(window.__STUDIO_AI_CREATOR_CARD_V2__)return;window.__STUDIO_AI_CREATOR_CARD_V2__=true;const esc=v=>window.App?.utils?.escapeHtml?window.App.utils.escapeHtml(v):String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#039;'}[c]));const norm=v=>String(v||'').trim().toLowerCase();const onStudio=()=>location.pathname.replace(/\\/+$/,'')==='/studio-ai';let timer=null,last='';const css=`.studio-premium-card{overflow:hidden;border:1px solid #e5e7eb;border-radius:30px;background:#fff;box-shadow:0 14px 34px rgba(15,23,42,.07);transition:transform .28s ease,box-shadow .28s ease,border-color .28s ease}.studio-premium-card:hover{transform:translateY(-5px);box-shadow:0 26px 60px rgba(21,28,117,.14);border-color:#d8def8}.studio-premium-media{position:relative;aspect-ratio:1;overflow:hidden;margin:12px;border-radius:23px;background:linear-gradient(135deg,#eef3ff,#f8fafc)}.studio-premium-media img{width:100%;height:100%;object-fit:cover;display:block;transition:transform .55s ease}.studio-premium-card:hover .studio-premium-media img{transform:scale(1.045)}.studio-premium-badge{position:absolute;top:14px;left:14px;z-index:2;max-width:calc(100% - 62px);padding:8px 11px;border-radius:999px;background:rgba(255,255,255,.94);border:1px solid #e2e8f0;color:#151c75;font-size:10px;font-weight:900;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.studio-premium-check{position:absolute;top:14px;right:14px;z-index:3;width:36px;height:36px;display:grid;place-items:center;border-radius:50%;background:#fff;color:#2563eb;border:1px solid #e2e8f0;box-shadow:0 8px 22px rgba(15,23,42,.14)}.studio-premium-body{padding:4px 17px 17px}.studio-premium-name{display:flex;align-items:center;gap:7px;color:#111827;font-size:18px;font-weight:900;letter-spacing:-.025em;text-decoration:none}.studio-premium-name-check{color:#2563eb;font-size:13px}.studio-premium-desc{margin:8px 0 0;min-height:40px;color:#7a8798;font-size:12px;line-height:1.55;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}.studio-premium-bottom{display:grid;grid-template-columns:minmax(0,1fr) auto;align-items:center;gap:10px;margin-top:16px}.studio-premium-pricebox{display:flex;align-items:center;gap:10px;min-width:0}.studio-premium-price{display:inline-flex;align-items:center;min-height:40px;padding:0 14px;border-radius:999px;background:#f3f4f6;color:#111827;font-size:18px;font-weight:900;white-space:nowrap}.studio-premium-likes{display:inline-flex;align-items:center;gap:4px;color:#64748b;font-size:11px;font-weight:800}.studio-premium-likes i{color:#ef4444;font-size:10px}.studio-premium-profile{display:inline-flex;align-items:center;justify-content:center;gap:7px;min-height:40px;padding:0 15px;border-radius:999px;color:#fff;background:#111827;border:1px solid #111827;text-decoration:none;font-size:12px;font-weight:900;transition:.2s}.studio-premium-profile:hover{transform:translateY(-1px);background:#151c75;border-color:#151c75}.studio-premium-avatar{width:100%;height:100%;display:grid;place-items:center;color:#151c75;font-size:34px;font-weight:900;background:radial-gradient(circle at 30% 20%,#fff,#e7edff 72%)}@media(max-width:640px){.studio-premium-bottom{grid-template-columns:1fr}.studio-premium-profile{width:100%}}`;function style(){if(document.getElementById('studio-premium-card-style-v2'))return;const s=document.createElement('style');s.id='studio-premium-card-style-v2';s.textContent=css;document.head.appendChild(s)}async function render(){if(!onStudio())return;const g=document.getElementById('studio-ai-creators-grid'),a=window.App?.studioAI;if(!g||!a)return;const items=(a._smartLastResults||a._smartResults||[]).slice(0,Number(a._creatorDisplayLimit||12));if(!items.length)return;style();const ids=items.map(x=>x.id).filter(Boolean),cats=new Map();try{const q=await window.supabaseClient.from('creator_category_members').select('creator_id,is_primary,ai_categories(name)').in('creator_id',ids);for(const row of q.data||[]){if(row.ai_categories?.name&&(!cats.has(row.creator_id)||row.is_primary===true))cats.set(row.creator_id,row.ai_categories.name)}}catch(_){}const sig=items.map(x=>[x.id,x.avatar_url,x.is_verified,x.likes,x.price_from,x.bio,cats.get(x.id)||x.primary_category].join('|')).join('||');if(sig===last&&g.querySelector('.studio-premium-card'))return;last=sig;g.innerHTML=items.map(c=>{const n=c.display_name||norm(c.username)||'Creator',h=norm(c.username),cat=cats.get(c.id)||c.primary_category||'Creator',d=c.bio||'Creator Studihome siap membantu kebutuhanmu.',p=Number(c.price_from||c.services?.[0]?.price_from||0),l=Number(c.likes||0),href=h?'/'+encodeURIComponent(h):'#',im=c.avatar_url?`<img src=\"${esc(c.avatar_url)}\" alt=\"Foto profil ${esc(n)}\" loading=\"lazy\" decoding=\"async\">`:`<div class=\"studio-premium-avatar\">${esc(n.charAt(0).toUpperCase())}</div>`;return `<article class=\"studio-premium-card\"><a href=\"${href}\"><div class=\"studio-premium-media\">${im}<span class=\"studio-premium-badge\">${esc(cat)}</span>${c.is_verified?'<span class=\"studio-premium-check\" title=\"Creator terverifikasi\"><i class=\"fa-solid fa-check\"></i></span>':''}</div></a><div class=\"studio-premium-body\"><a class=\"studio-premium-name\" href=\"${href}\">${esc(n)}${c.is_verified?'<i class=\"studio-premium-name-check fa-solid fa-circle-check\"></i>':''}</a><p class=\"studio-premium-desc\">${esc(d)}</p><div class=\"studio-premium-bottom\"><div class=\"studio-premium-pricebox\"><strong class=\"studio-premium-price\">${p>0?'Rp '+p.toLocaleString('id-ID'):'Hubungi Creator'}</strong><span class=\"studio-premium-likes\"><i class=\"fa-solid fa-heart\"></i>${l.toLocaleString('id-ID')}</span></div><a class=\"studio-premium-profile\" href=\"${href}\">Profil <i class=\"fa-solid fa-arrow-up-right-from-square\"></i></a></div></div></article>`}).join('')}function boot(){style();render();new MutationObserver(()=>{clearTimeout(timer);timer=setTimeout(()=>render(),120)}).observe(document.body,{childList:true,subtree:true})}window.StudioAICreatorCard={render};document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot,{once:true}):boot()})();""",encoding='utf-8')

          dpe=root/'dapur-profile-enhancements.js'
          dpe.write_text("""(()=>{'use strict';if(window.__DAPUR_PROFILE_ENHANCEMENTS_V2__)return;window.__DAPUR_PROFILE_ENHANCEMENTS_V2__=true;const db=()=>window.supabaseClient,esc=v=>window.App?.utils?.escapeHtml?window.App.utils.escapeHtml(v):String(v??''),toast=(m,k='info')=>window.App?.ui?.toast?window.App.ui.toast(m,k):alert(m),path=()=>location.pathname.replace(/\\/+$/,'')||'/',onDapur=()=>/^\\/dapur(?:\\/[a-z0-9][a-z0-9-]{2,39})?$/i.test(path());const css=`.dpe-overlay{position:fixed;inset:0;z-index:12000;display:flex;align-items:center;justify-content:center;padding:16px;background:rgba(15,23,42,.62);backdrop-filter:blur(12px)}.dpe-modal{width:min(620px,100%);max-height:92dvh;overflow:auto;border:1px solid #dbe5f5;border-radius:30px;background:#fff;box-shadow:0 36px 100px rgba(15,23,42,.28)}.dpe-head{padding:20px 22px 16px;border-bottom:1px solid #e8edf5;background:linear-gradient(145deg,#f7f9ff,#fff)}.dpe-kicker{color:#667eea;font-size:9px;font-weight:900;letter-spacing:.12em;text-transform:uppercase}.dpe-title{margin:7px 0;color:#151c75;font-size:23px;font-weight:900}.dpe-sub{color:#64748b;font-size:12px;line-height:1.6}.dpe-body{padding:20px 22px}.dpe-avatar{width:116px;height:116px;margin:0 auto 13px;border-radius:32px;overflow:hidden;background:#eef3ff;display:grid;place-items:center;color:#151c75;font-size:36px;font-weight:900;border:4px solid #fff;box-shadow:0 14px 30px rgba(21,28,117,.14)}.dpe-avatar img{width:100%;height:100%;object-fit:cover}.dpe-upload{display:inline-flex;gap:7px;align-items:center;padding:10px 14px;border-radius:13px;background:#151c75;color:#fff;font-size:11px;font-weight:900;cursor:pointer}.dpe-help{text-align:center;color:#94a3b8;font-size:10px;margin-top:7px}.dpe-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:16px}.dpe-field.full{grid-column:1/-1}.dpe-label{display:block;color:#334155;font-size:11px;font-weight:900}.dpe-input{width:100%;min-height:48px;margin-top:7px;padding:11px 13px;border:1px solid #d6dfef;border-radius:14px;color:#182238;background:#f8fafc;font-size:16px;outline:0}.dpe-input:focus{border-color:#667eea;box-shadow:0 0 0 3px rgba(102,126,234,.1);background:#fff}.dpe-input:disabled{color:#64748b;background:#f1f5f9}.dpe-note{margin-top:9px;padding:10px 12px;border-radius:14px;background:#fff8e7;border:1px solid #f6df9e;color:#72520a;font-size:10px;line-height:1.55}.dpe-footer{display:flex;justify-content:flex-end;gap:9px;padding:14px 22px;border-top:1px solid #e8edf5;background:#f8fafc}.dpe-btn{min-height:42px;padding:10px 15px;border:1px solid #dbe3ef;border-radius:13px;background:#fff;color:#334155;font-size:12px;font-weight:900;cursor:pointer}.dpe-btn.primary{border:0;background:linear-gradient(135deg,#151c75,#3f48bf);color:#fff}.dpe-btn:disabled{opacity:.6;cursor:not-allowed}@media(max-width:560px){.dpe-grid{grid-template-columns:1fr}.dpe-field.full{grid-column:auto}.dpe-footer .dpe-btn{flex:1}}`;function style(){if(document.getElementById('dpe-style-v2'))return;const s=document.createElement('style');s.id='dpe-style-v2';s.textContent=css;document.head.appendChild(s)}function normalize(v){return String(v||'').trim().toLowerCase().replace(/[^a-z0-9-]/g,'-').replace(/-+/g,'-').replace(/^-|-$/g,'').slice(0,40)}async function optimize(file){if(!file||!/^image\\/(jpeg|png|webp)$/i.test(file.type))throw Error('Pakai foto JPG, PNG, atau WebP.');if(file.size>8*1024*1024)throw Error('Foto maksimal 8 MB sebelum diproses.');const img=await new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>{const i=new Image();i.onload=()=>resolve(i);i.onerror=reject;i.src=r.result};r.onerror=reject;r.readAsDataURL(file)});const scale=Math.min(1,640/Math.max(img.naturalWidth,img.naturalHeight)),c=document.createElement('canvas');c.width=Math.max(1,Math.round(img.naturalWidth*scale));c.height=Math.max(1,Math.round(img.naturalHeight*scale));c.getContext('2d').drawImage(img,0,0,c.width,c.height);for(const q of [.82,.74,.66,.58,.5]){const b=await new Promise(r=>c.toBlob(r,'image/webp',q));if(b&&b.size<=400*1024)return new File([b],'avatar.webp',{type:'image/webp'})}throw Error('Foto masih terlalu besar. Pilih foto yang lebih sederhana.')}async function open(){style();try{const auth=await db().auth.getUser();if(auth.error||!auth.data.user)throw Error('Silakan masuk terlebih dahulu.');const id=auth.data.user.id,r=await db().from('creator_profiles').select('id,user_id,username,display_name,bio,avatar_url,username_changed_at').eq('user_id',id).maybeSingle();if(r.error||!r.data)throw r.error||Error('Profil Creator belum tersedia.');const x=r.data,locked=Boolean(x.username_changed_at);let avatar=x.avatar_url||'';const m=document.createElement('div');m.className='dpe-overlay';m.innerHTML=`<div class=\"dpe-modal\"><header class=\"dpe-head\"><button id=\"dpe-x\" type=\"button\" style=\"float:right;width:38px;height:38px;border:1px solid #dbe3ef;border-radius:12px;background:#fff;color:#151c75\">×</button><div class=\"dpe-kicker\">Dapur Creator · Foyer</div><h2 class=\"dpe-title\">Rapikan profilmu ✨</h2><p class=\"dpe-sub\">Foto, nama, username, dan bio akan tampil di profil publik Creator.</p></header><form class=\"dpe-body\" id=\"dpe-f\"><div class=\"dpe-avatar\" id=\"dpe-a\">${avatar?`<img src=\"${esc(avatar)}\" alt=\"Foto profil\">`:esc((x.display_name||x.username||'C').charAt(0).toUpperCase())}</div><div style=\"text-align:center\"><label class=\"dpe-upload\" for=\"dpe-file\"><i class=\"fa-solid fa-camera\"></i> Ganti foto profil</label><input id=\"dpe-file\" type=\"file\" accept=\"image/jpeg,image/png,image/webp\" hidden><div class=\"dpe-help\">JPG/PNG/WebP · otomatis WebP ≤ 400 KB</div></div><div class=\"dpe-grid\"><div class=\"dpe-field\"><label class=\"dpe-label\">Nama tampilan<input class=\"dpe-input\" name=\"name\" maxlength=\"80\" value=\"${esc(x.display_name||'')}\" required></label></div><div class=\"dpe-field\"><label class=\"dpe-label\">Username publik<input class=\"dpe-input\" name=\"user\" maxlength=\"40\" value=\"${esc(x.username||'')}\" ${locked?'disabled':''}></label><div class=\"dpe-note\">${locked?'Username sudah dikunci permanen.':'Username hanya dapat diubah satu kali.'}</div></div><div class=\"dpe-field full\"><label class=\"dpe-label\">Bio singkat<textarea class=\"dpe-input\" name=\"bio\" maxlength=\"500\" rows=\"4\">${esc(x.bio||'')}</textarea></label></div></div></form><footer class=\"dpe-footer\"><button class=\"dpe-btn\" id=\"dpe-c\" type=\"button\">Batal</button><button class=\"dpe-btn primary\" id=\"dpe-s\" type=\"submit\" form=\"dpe-f\">Simpan perubahan</button></footer></div>`;document.body.appendChild(m);const form=m.querySelector('#dpe-f'),file=m.querySelector('#dpe-file'),box=m.querySelector('#dpe-a'),save=m.querySelector('#dpe-s'),close=()=>m.remove();m.querySelector('#dpe-x').onclick=close;m.querySelector('#dpe-c').onclick=close;file.onchange=async()=>{const picked=file.files?.[0];if(!picked)return;file.disabled=true;try{const optimized=await optimize(picked),storage=db().storage.from('creator-media'),path=`${id}/avatar.webp`,up=await storage.upload(path,optimized,{contentType:'image/webp',cacheControl:'31536000',upsert:true});if(up.error)throw up.error;avatar=`${storage.getPublicUrl(path).data.publicUrl}?v=${Date.now()}`;box.innerHTML=`<img src=\"${esc(avatar)}\" alt=\"Foto profil baru\">`;toast('Foto profil siap disimpan ✨','success')}catch(err){toast(err?.message||'Foto belum bisa diunggah.','error')}finally{file.disabled=false;file.value=''}};form.onsubmit=async ev=>{ev.preventDefault();save.disabled=true;try{const old=x.username,next=locked?old:normalize(form.user.value);if(next.length<3)throw Error('Username minimal 3 karakter.');if(!locked&&next!==old){const q=await db().rpc('change_creator_username_once',{p_username:next});if(q.error)throw q.error}const q=await db().from('creator_profiles').update({display_name:form.name.value.trim(),bio:form.bio.value.trim(),avatar_url:avatar||null}).eq('id',x.id).eq('user_id',id);if(q.error)throw q.error;const changed=!locked&&next!==old;toast('Profil berhasil diperbarui. ✨','success');close();setTimeout(()=>{location.href=changed?`/dapur/${encodeURIComponent(next)}`:location.href},320)}catch(err){const msg=String(err?.message||'');toast(msg.includes('USERNAME_ALREADY_CHANGED')?'Username sudah pernah diubah.':msg.includes('USERNAME_ALREADY_TAKEN')?'Username itu sudah digunakan.':msg||'Perubahan belum bisa disimpan.','error')}finally{save.disabled=false}}}catch(err){toast(err?.message||'Profil belum bisa dibuka.','error')}}function boot(){if(!onDapur())return;const h=document.querySelector('.workhero .acts');if(!h||h.querySelector('[data-dpe]'))return;const b=document.createElement('button');b.className='btn primary';b.type='button';b.dataset.dpe='1';b.innerHTML='<i class=\"fa-solid fa-wand-magic-sparkles mr-1\"></i> Edit Profil';b.setAttribute('aria-label','Edit Profil / Foyer');b.onclick=open;h.prepend(b)}new MutationObserver(boot).observe(document.body,{childList:true,subtree:true});document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot,{once:true}):boot();window.DapurProfileEnhancements={open}})();""",encoding='utf-8')

          entry=root/'dapur-entry.js';s=entry.read_text(encoding='utf-8')
          s=s.replace("const sections=[['Foyer','Profil, bio, kontak, dan status tayang.'],['Menu','Kategori dan fokus keahlian.'],['Hidangan','Layanan, harga, manfaat, dan estimasi.'],['Ambalan','Portofolio dan bukti kerja.']];","const sections=[['Foyer','Profil utama & kontak.','Rapikan identitas Creator-mu.','fa-regular fa-user'],['Menu','Kategori & keahlian.','Pilih fokus yang paling kuat.','fa-solid fa-compass'],['Hidangan','Layanan & harga.','Tampilkan apa yang bisa kamu bantu.','fa-solid fa-utensils'],['Ambalan','Karya & bukti.','Pasang karya yang paling meyakinkan.','fa-solid fa-layer-group']];",1)
          old='<section class="aside"><section class="card section" style="margin-top:0"><div class="head"><div><h2>Tips cepat cari customer</h2><p>Gunakan Dapur sebagai halaman konversi sederhana.</p></div></div><div class="tip" style="margin-top:15px"><p><b>1.</b> Pilih satu layanan unggulan dulu.</p><p><b>2.</b> Tampilkan 1–3 karya paling kuat.</p><p><b>3.</b> Tempel link Dapur di bio, status WhatsApp, TikTok, Instagram, email, dan proposal.</p><p><b>4.</b> Arahkan customer ke satu aksi utama: chat, order, atau minta penawaran.</p></div></section><aside class="card tip" style="margin-top:0"><h3>Fokus hari ini</h3><p>Profil jelas + satu layanan + beberapa karya sudah cukup untuk mulai dibagikan.</p></aside></section>'
          if old not in s: raise SystemExit('legacy aside not found')
          s=s.replace(old,'',1)
          s=s.replace('<h2>Langkah utama</h2><p>Empat bagian inti yang membentuk profil Creator-mu.</p>','<h2>Rapikan Dapurmu</h2><p>4 bagian inti biar profilmu makin siap dilihat.</p>',1)
          s=s.replace('<h2>Link publik resmi</h2><p>Gunakan alamat publik ini untuk customer. Workspace tetap berada di /dapur/${esc(c.username)}.</p>','<h2>Link siap dibagikan</h2><p>Kirim link ini ke customer. Workspace kamu tetap aman di <span style="white-space:nowrap">/dapur/${esc(c.username)}</span>.</p>',1)
          oldmap='${sections.map((x,i)=>`<button type="button" class="step ${done[i]?\'done\':\'\'}" data-step="${i}"><div class="sttop"><i>${done[i]?\'✓\':String(i+1).padStart(2,\'0\')}</i><span class="state">${done[i]?\'Siap\':\'Perlu diisi\'}</span></div><h3>${x[0]}</h3><p>${x[1]}</p><span class="link">${done[i]?\'Edit bagian ini →\':\'Lengkapi sekarang →\'}</span></button>`).join(\'\')}'
          newmap='${sections.map((x,i)=>`<button type="button" class="step ${done[i]?\'done\':\'\'}" data-step="${i}"><div class="sttop"><i class="icon">${done[i]?\'<i class="fa-solid fa-check"></i>\':`<i class="${x[3]}"></i>`}</i><span class="state">${done[i]?\'Siap\':\'Belum\'}</span></div><h3>${x[0]}</h3><p>${x[1]}</p><p class="step-desc">${x[2]}</p><span class="link">${done[i]?\'Edit lagi ↗\':\'Buka sekarang ↗\'}</span></button>`).join(\'\')}'
          if oldmap not in s: raise SystemExit('legacy steps map not found')
          s=s.replace(oldmap,newmap,1)
          i=root/'index.html';t=i.read_text(encoding='utf-8').replace('/studio-ai-creator-card.js?v=1','/studio-ai-creator-card.js?v=2').replace('/dapur-profile-enhancements.js?v=1','/dapur-profile-enhancements.js?v=2');i.write_text(t,encoding='utf-8')
          entry.write_text(s,encoding='utf-8')
      - name: Validate
        shell: bash
        run: |
          set -e
          node --check studio-ai-creator-card.js
          node --check dapur-profile-enhancements.js
          node --check dapur-entry.js
          ! grep -q 'Tips cepat cari customer' dapur-entry.js
          ! grep -q 'Fokus hari ini' dapur-entry.js
          git diff --check
      - name: Commit and remove workflow
        shell: bash
        run: |
          set -e
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/refine-studio-dapur-ui.yml
          git add index.html studio-ai-creator-card.js dapur-profile-enhancements.js dapur-entry.js
          git diff --cached --check
          git commit -m "feat: refine Studio AI creator card and Dapur UX"
          git push