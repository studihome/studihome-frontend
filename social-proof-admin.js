(() => {
  'use strict';
  // Social Proof Admin — renderSocialProof is now defined directly
  // in App.admin (index.html) for guaranteed availability.
  // This file is kept as a no-op for script tag compatibility.
})();
